return {
    init = function()
    end,
    dap_config = {
        adapter = '',
        adapter_config = {

        },
        default_configs = { {

        } },
    },
    lsp_config = {
        lsp = '',
        config = {},
    },
    repl_config = function(meta)
        return { '' }
    end,
    theme = function(c)
    end,
}

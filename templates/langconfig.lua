return {
    dap_config = {
        adapter = '',
        adapter_config = {

        },
        default_configs = { {

        } },
    },
    lsp_config = {
        lsp = '',
        config = {},
    },
    repl_config = function(meta)
        return { '' }
    end,
    key_mappings = {

    },
    theme = function(c)

    end,
}

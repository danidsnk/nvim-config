return {
    dap_config = {
        adapter = '',
        adapter_config = {

        },
        default_configs = { {

        } }
    },
    lsp_config = {

    },
    repl_config = function(meta)
        return { '' }
    end,
    key_mappings = {

    },
    theme = {

    },
}
